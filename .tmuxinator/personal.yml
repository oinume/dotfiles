name: personal
root: ~/
windows:
  - dotfiles:
      layout: main-vertical
      panes:
        - cd ~/dotfiles
  - myservers:
      layout: main-vertical
      panes:
        - cd ~/workspace/oinume/myservers
  - terraforms:
      layout: main-vertical
      panes:
        - cd ~/workspace/oinume/terraforms
